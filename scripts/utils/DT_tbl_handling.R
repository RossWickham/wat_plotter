

#test data
blankFileConfig <- data.frame(filePath=NA,fileType=NA,legName=NA,col=NA,stringsAsFactors = F)
# fileConfig = data.frame(bins = c(30, 50), cb = c(T, F))
#initializing with blank
fileConfig <- blankFileConfig 

